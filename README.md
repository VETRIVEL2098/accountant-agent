# 🧾 AI Accountant Agent (Flask + OpenAI + LangChain)

An agentic AI that analyzes company transactions, categorizes expenses, detects anomalies, and generates human-like financial insights.

## 🧠 Features
- Flask-based backend API
- Pandas + LangChain + OpenAI integration
- Real-time INR/USD conversion
- Human-style insights via GPT reasoning

## ⚙️ Setup
```bash
git clone https://github.com/<your-username>/ai-accountant-agent.git
cd ai-accountant-agent
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
